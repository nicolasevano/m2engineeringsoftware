package state.resize;

import java.awt.Point;

import command.Command;

import state.State;

public class ResizeEnd implements State{

	@Override
	public void draw(Point p, Point origine, Command currentCommand) {
		// TODO Auto-generated method stub
		//currentCommand.getDessin().removeAll () ;
		//currentCommand.getDessin().validate () ;
		//( ( ZoneDeDessin )currentCommand.getPlan()).repaint();
		//( ( ZoneDeDessin )currentCommand.getPlan()).setSelected( null );
		//currentCommand.setDessin( null );
		
	}
	
}
